apiVersion: v1
kind: Service
metadata:
  name: product-service
spec:
  ports:
  - port: 5000
    protocol: TCP
    targetPort: 5000
  selector:
    app: product-service
  type: LoadBalancer