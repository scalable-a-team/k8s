apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: app-jwt-auth-only
plugin: jwt

---

apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: app-jwt-public
config:
  anonymous: anon
plugin: jwt
